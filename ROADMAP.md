# 通用项目级业务审计路线图

这份路线图解决 Code Doctor 只能生成单场景地图、尚不能让新 Owner 仅凭一个 HTML 掌握项目全局并定位业务断链的问题。

## 最终验收

- 在 TS、JS、Vue、Go 项目中通过通用 CLI 生成一个自包含项目报告，不依赖诊室项目硬编码。
- 报告明确区分已扫描、已发现、已建图、已确认、证据不足、未覆盖和已过期，绝不宣称“覆盖全部业务”。
- 从项目业务场景下钻到章节、链路、路径条件、风险和源码证据。
- 自动检测确定性断链，并由 Agent 对业务逻辑风险进行证据化复核。
- 支持中断恢复、Git 增量更新和 CI Artifact。
- 在诊室真实项目和至少两个独立夹具项目完成验证。

## TODO

- [x] M1 项目级业务总览协议、聚合 HTML 与 `project build/open` CLI
- [x] M2 可恢复的全场景逐一建图与覆盖/未知边界统计
- [x] M3 确定性断链检测与 AI 全局业务风险审计聚合
- [x] M4 Git 变更影响分析、场景增量重建与 CI 产物
- [x] M5 一键使用体验、Agent 兼容与跨项目夹具测试
- [x] M6 诊室真实项目全量验证、可用性验收与发布

## 通关证据

每个里程碑只有在实现、自动化测试和真实产物验证全部完成后才能勾选；仅提交代码不算完成。

- M1：`npm run check` 通过（9 个测试文件、18 个测试）；诊室项目生成 15 个候选场景、1 个已建图、0 个已确认的自包含 `project-report.html`，并明确展示 8 项未知边界。
- M2：`npm run check` 通过（10 个测试文件、22 个测试）；诊室项目用 `project build --all --limit 1` 串行新增“断网与恢复”地图，累计 2/15，剩余 13 个明确标为待处理；新增地图含 30 个节点、31 条路径和 30 条证据。
- M3：`npm run check` 通过（11 个测试文件、25 个测试）；诊室 2/15 场景真实 AI 审计生成 6 个结构复核项和 6 个业务语义候选，其中 4 项满足节点与强证据门槛标为 risk，13 个未建图场景继续明确显示为盲区。
- M4：`npm run check` 通过（12 个测试文件、30 个测试）；Git 参数使用无 shell 的参数数组，覆盖删除/重命名、未归属和恢复状态；诊室真实 `HEAD...HEAD` 验证为 0 变更且未误重建地图，impact 与 AI 审计聚合进同一 HTML；Daily CI 增加跨 Pipeline 知识与进度缓存。
- M5：`npm run check` 通过（13 个测试文件、33 个测试）；在仓库外临时目录真实安装 npm tarball，独立 TS/JS/Vue、Go、空项目/无 Agent 均跑通 CLI 与单 HTML；新增 `project start`、`project doctor --json`，诊室自检正确报告 15/2/0 覆盖与可用 Agent。
- M6：诊室真实项目完成 15/15 场景建图，累计 88 个章节、417 个节点、477 条路径、562 条证据，`buildPending=0`、`buildFailed=0`；全局 AI 审计成功并识别 10 项有证据风险、64 项待复核。最终 `npm run check` 通过（13 个测试文件、34 个测试及真实 tarball 安装 E2E），Tesla 业务源码无新增修改；`@thunder-doctor/code-doctor@0.5.0` 已公开发布并从公共 registry 安装验证通过。
