## Design Context

### Users

主要用户是刚接手历史项目的业务 Owner、研发 Owner 和代码审计者。他们对历史业务不熟悉，需要从代码反推业务全貌，沿一条可执行链路理解前置条件、判断分支、跨端调用、结果和副作用，并能从业务节点下钻到源码证据。

### Brand Personality

清晰、可信、克制。界面应让使用者感到自己面对的是一张有证据边界的业务审计底图，而不是炫技式技术大屏。AI 推断必须明显区别于代码事实和人工确认，冲突与未知不能被视觉效果掩盖。

### Aesthetic Direction

采用工程化业务审计台风格，以可无限平移缩放的浅色网格画布为主体，配合高对比但低刺激的中性色。节点通过形状、图标和结构表达类型；证据状态通过标签与线型表达；审计风险使用独立角标。参考 React Flow 的节点画布交互，但避免默认示例式的通用卡片、霓虹深色科技感和只靠颜色区分状态。

### Design Principles

1. 先看业务章节全景，再下钻局部场景、完整链路和代码证据；默认屏蔽函数级噪声。
2. 节点类型、证据状态、审计结果使用三套独立视觉语义，不让绿色等同于“没有问题”。
3. 选择节点或连线时突出完整上下游链路，其余内容明显降噪，但始终可以一键恢复全景。
4. 所有颜色含义同时使用文字、图标、形状或线型表达，保证色觉差异用户仍能理解。
5. 地图是只读、可追溯的审计产物；布局可以临时调整，但不能冒充业务事实写回知识库。
