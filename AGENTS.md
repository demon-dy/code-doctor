# Code Doctor 协作约定

本项目解决历史代码问题只能堆积、缺少持续小步修复，以及客户端到服务端调用链难以理解的问题。

- 始终使用中文沟通，代码标识符使用英文。
- 修改必须保持 CLI 的非交互模式可用。
- 扫描器负责确定性发现，Agent 负责解释和最小修复；不要让模型输出替代扫描证据。
- 新增诊断格式、调用图格式时必须补兼容性测试。
- 自动修复一次只能选择一个诊断，禁止自动合并 MR。
- 不提交密钥、访问令牌、Agent 完整上下文或被扫描项目源码。
- npm 包只能发布到 `g.ktvsky.com` 内部 Registry，禁止发布到 npmjs 或其他外部仓库。
- 提交信息格式：`类型: 中文说明`，不添加 AI 署名。
